#encontrando el numero mayor de una lista,tupla,conjunto con funcion max

list = [1,2,3,4,5]

#mayor 
numero_mayor = max(list)
#menor
numero_menor = min(list)

#redodenadndo con decimales. antes mencione que podemos mostrar la cantidad de decimales con una vuelta toda ñarga pero con este metodo es mucho mas facil y mejor 

numero = round(10.923853,3)#lo que hace round es que podemos seleccionar cuantos decimales quiero mostrar. con ,3 si pongo ,2 pues me muestra dos, asin de facil

#retornar con false -> 0, none, False, vacio / True -> distinto a 0, True, cadenas de texto, datos no vacios 
boleano = bool("dwefw")

#retorna true si todos los valores son verdaderos 
valores_verdaderos = all([1,12,42,55,0,32])

#suma todos los valores de un iterable 
suma_total = sum(list)
